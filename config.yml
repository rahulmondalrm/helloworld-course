title: hello/world
description: >-
  "Hello World" Generally it is a computer program which displays and output the
  message "Hello World". It is the simplest program. It is the first program
  which is often written by the programmers. 
template:
  repo: helloworld-template
  name: helloworld
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
